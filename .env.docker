# HOSHINO Blog · Docker 部署环境变量
# 复制为 .env 后修改，docker compose up -d 启动

# --- 密钥（必改）---
SECRET_KEY=openssl rand -hex 32 的输出
ADMIN_PASSWORD=你的管理员密码

# --- 可选 ---
# SCRAPING_PROXY=http://user:pass@host:port
# EXA_API_KEY=你的 Exa API Key
